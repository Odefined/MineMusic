// Workbench Interface currently has no exported in-process agent composition
// seam. Agent Runtime consumes Workbench-owned interaction-state facts through
// dedicated ports when those facts exist.
