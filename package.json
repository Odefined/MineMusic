{
  "name": "minemusic",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run typecheck && npm run test:runtime",
    "test:runtime": "npm run build:test && node .tmp-test/test/storage/in-memory-repositories.test.js && node .tmp-test/test/plugins/plugin-registry.test.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
